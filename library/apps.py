"""
Module for configuring the application.
"""
from django.apps import AppConfig

class LibraryConfig(AppConfig):
    """
    AppConfig class for configuring the library application.
    """
    name = 'library'
